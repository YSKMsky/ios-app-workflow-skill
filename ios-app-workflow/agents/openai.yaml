interface:
  display_name: "iOS App Workflow"
  short_description: "Guide non-coders through staged SwiftUI apps"
  default_prompt: "Use $ios-app-workflow to guide a non-coding user through SwiftUI app scope, short docs, UI reference, implementation planning, risk-driven testing, skill routing, and handoff."

policy:
  allow_implicit_invocation: true
